import 'package:flutter/material.dart';

class GPA_Report extends StatelessWidget {
  const GPA_Report({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("GPA calculate")),
    );
  }
}
